import 'package:flutter/material.dart';

class HelpsScreen extends StatelessWidget {
  const HelpsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Yardımlar Sayfası'),
      ),
    );
  }
}
